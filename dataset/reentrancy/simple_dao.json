[
  {
    "vuln_id": "manual_reentrancy_simpledao_001",
    "vuln_type": "reentrancy",
    "tool": "manual_dataset",
    "severity": "High",
    "title": "State Update After External Call (Reentrancy)",
    "description": "The `withdraw` function makes an external call to `msg.sender` sending Ether before updating the user's credit balance. A malicious contract can recursively call `withdraw()` during its fallback/receive execution to repeatedly bypass the balance check and drain the contract's funds.",
    "location": {
      "file": "simple_dao.sol",
      "contract": "SimpleDAO",
      "function": "withdraw",
      "lines": [15, 21]
    },
    "code_snippet": "function withdraw(uint amount) public {\n  if (credit[msg.sender]>= amount) {\n    (bool res, ) = msg.sender.call{value: amount}(\"\");\n    credit[msg.sender]-=amount;\n  }\n}"
  }
]